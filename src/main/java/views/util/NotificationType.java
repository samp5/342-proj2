package views.util;

/*
 * NOTE:
 * When we add to this type, make sure to update the corresponding switch patterns 
 * in NotificationBuilder so that the correct style classes are applied
 */
public enum NotificationType {
  Error,
  Info,
}
